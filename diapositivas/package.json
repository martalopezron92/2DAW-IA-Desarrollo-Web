{
  "name": "slides-modelos-supervisados",
  "version": "1.0.0",
  "private": true,
  "description": "Presentación en Marp: Modelos supervisados (clasificación y regresión)",
  "scripts": {
    "build": "marp slides/U6-Modelos-Supervisados.md --config marp.config.js --html --output dist/index.html",
    "build:pdf": "marp -I slides --config marp.config.js --pdf --output dist",
    "dev": "marp -I slides --config marp.config.js --server --watch"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^4.1.2"
  }
}
