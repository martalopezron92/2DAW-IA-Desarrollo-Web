# EJERCICIOS GUIADOS DE CLASE
---

## 📝 Ejercicio 1: Primeros pasos con Pandas

### Contexto:
Tienes una lista de estudiantes con sus notas.

### Datos:
```python
import pandas as pd

datos = {
    'nombre': ['Ana', 'Pedro', 'María', 'Luis', 'Carmen'],
    'edad': [20, 22, 21, 23, 20],
    'nota': [7.5, 8.0, 6.5, 9.0, 7.0]
}
```

### Tareas:
1. Crea un DataFrame con estos datos
2. Muestra las primeras 3 filas
3. Calcula la nota media de la clase
4. Encuentra el estudiante con la nota más alta
5. Filtra estudiantes con nota >= 7.0

### Pistas:
- Usa `pd.DataFrame(datos)`
- Usa `.head(3)` para ver filas
- Usa `.mean()` para calcular media
- Usa `.max()` o `.sort_values()`
- Filtra con `df[df['nota'] >= 7.0]`

---

## 📝 Ejercicio 2: Análisis de ventas de una tienda

### Contexto:
Una tienda online quiere analizar sus ventas del último mes.

### Cargar datos:
```python
import pandas as pd

df = pd.read_csv('../datasets/ejemplo_datos.csv')
```

### Tareas:
1. Muestra información básica del dataset (info, describe)
2. ¿Cuántas ventas se realizaron en total?
3. ¿Cuál es el precio medio de los productos vendidos?
4. ¿Cuál es el producto más caro vendido?
5. Filtra las ventas de Madrid
6. Calcula el total de ventas por ciudad
7. Ordena las ciudades por ventas totales (de mayor a menor)

### Pistas:
- `.info()` y `.describe()` para info general
- `len(df)` para contar filas
- `.mean()`, `.max()` para estadísticas
- `groupby('ciudad')['precio'].sum()` para agrupar
- `sort_values(ascending=False)` para ordenar descendente

---

## 📝 Ejercicio 3: Limpieza de datos

### Contexto:
Has recibido un dataset con errores que debes limpiar.

### Datos sucios:
```python
datos_sucios = {
    'nombre': ['Ana García', ' Pedro López', 'María Ruiz', 'Ana García'],  # Espacios y duplicado
    'edad': [25, None, 30, 25],  # Valor nulo
    'email': ['ana@mail.com', 'pedro@mail.com', None, 'ana@mail.com'],  # Nulo y duplicado
    'ciudad': ['Madrid', 'barcelona', 'VALENCIA', 'Madrid']  # Formatos inconsistentes
}

df = pd.DataFrame(datos_sucios)
```

### Tareas:
1. Elimina espacios en blanco de la columna 'nombre'
2. Rellena los valores nulos de 'edad' con la media
3. Elimina filas con email nulo
4. Normaliza la columna 'ciudad' (primera letra mayúscula)
5. Elimina duplicados
6. Muestra el resultado final

### Pistas:
- `.str.strip()` para eliminar espacios
- `.fillna(df['edad'].mean())` para rellenar
- `.dropna(subset=['email'])` para eliminar nulos
- `.str.title()` para formato Título
- `.drop_duplicates()` para eliminar duplicados

---

## 📝 Ejercicio 4: Creación de nuevas columnas

### Contexto:
Tienes ventas y quieres calcular el total con IVA.

### Datos:
```python
ventas = {
    'producto': ['Laptop', 'Mouse', 'Teclado', 'Monitor'],
    'precio_base': [800, 20, 45, 200],
    'cantidad': [1, 3, 2, 1]
}

df = pd.DataFrame(ventas)
```

### Tareas:
1. Crea una columna 'subtotal' (precio_base × cantidad)
2. Crea una columna 'iva' (subtotal × 0.21)
3. Crea una columna 'total' (subtotal + iva)
4. Crea una columna 'categoria_precio':
   - 'Económico' si precio_base < 50
   - 'Medio' si precio_base < 300
   - 'Premium' si precio_base >= 300
5. Muestra el DataFrame final

### Pistas:
- `df['nueva_col'] = df['col1'] * df['col2']` para calcular
- Usa función con `.apply()` o `np.where()` para categorizar

---

## 📝 Ejercicio 5: Agrupaciones y resúmenes

### Contexto:
Analiza las ventas por categoría de producto.

### Datos:
```python
ventas = {
    'producto': ['Laptop', 'Mouse', 'Monitor', 'Teclado', 'Webcam', 'Auriculares'],
    'categoria': ['Informática', 'Accesorios', 'Informática', 'Accesorios', 'Accesorios', 'Accesorios'],
    'precio': [800, 20, 200, 45, 60, 35],
    'cantidad': [5, 30, 8, 15, 12, 20]
}

df = pd.DataFrame(ventas)
```

### Tareas:
1. Calcula el subtotal por producto (precio × cantidad)
2. Agrupa por categoría y calcula:
   - Total vendido por categoría
   - Precio medio por categoría
   - Cantidad de productos diferentes por categoría
3. ¿Qué categoría generó más ingresos?
4. ¿Cuál es el producto más vendido (en cantidad)?

### Pistas:
- Crea columna subtotal primero
- `groupby('categoria').agg({'subtotal': 'sum', 'precio': 'mean', 'producto': 'count'})`
- Usa `.sort_values()` para encontrar máximos

---

## 📝 Ejercicio 6: Visualización básica

### Contexto:
Crea gráficos para presentar los resultados del ejercicio anterior.

### Tareas:
1. Crea un gráfico de barras con el total vendido por categoría
2. Crea un gráfico de tarta mostrando el % de ventas por categoría
3. Crea un gráfico de barras horizontal con los productos ordenados por subtotal
4. Personaliza los gráficos con:
   - Títulos claros
   - Etiquetas en los ejes
   - Colores apropiados

### Pistas:
```python
import matplotlib.pyplot as plt

# Gráfico de barras
plt.bar(x, y)
plt.title('Título')
plt.xlabel('Etiqueta X')
plt.ylabel('Etiqueta Y')
plt.show()

# Gráfico de tarta
plt.pie(valores, labels=etiquetas, autopct='%1.1f%%')
plt.show()
```

---

## 📝 Ejercicio 7: Análisis temporal

### Contexto:
Analiza la evolución de ventas a lo largo del tiempo.

### Datos:
```python
ventas_tiempo = {
    'fecha': ['2024-01-15', '2024-01-16', '2024-01-17', '2024-01-18', '2024-01-19'],
    'ventas': [1200, 1500, 1350, 1800, 2100]
}

df = pd.DataFrame(ventas_tiempo)
```

### Tareas:
1. Convierte la columna 'fecha' a tipo datetime
2. Extrae el día de la semana de cada fecha
3. Calcula el total de ventas
4. Calcula el crecimiento porcentual día a día
5. Crea un gráfico de líneas mostrando la evolución
6. Añade marcadores en cada punto

### Pistas:
- `pd.to_datetime(df['fecha'])` para convertir
- `.dt.day_name()` para nombre del día
- Crecimiento: `((nuevo - anterior) / anterior) * 100`
- `plt.plot(x, y, marker='o')` para gráfico con marcadores

---

## 📝 Ejercicio 8: Proyecto integrador

### Contexto:
Análisis completo de datos de una cafetería.

### Dataset:
Usa `../datasets/ejemplo_datos.csv` o crea uno propio con:
- producto, precio, cantidad, fecha, ciudad

### Tareas:
1. **Carga y exploración**:
   - Carga el dataset
   - Muestra info básica
   - Revisa valores nulos

2. **Limpieza**:
   - Trata valores nulos
   - Elimina duplicados
   - Normaliza formatos

3. **Transformación**:
   - Crea columna 'total'
   - Extrae mes de la fecha
   - Categoriza productos por precio

4. **Análisis**:
   - Ventas totales
   - Producto más vendido
   - Ciudad con más ventas
   - Mes con más ingresos

5. **Visualización**:
   - Dashboard con 4 gráficos
   - Presentación clara

---

# EJERCICIOS DE REFUERZO


## 📝 Ejercicio 1: Análisis de películas

### Contexto:
Tienes información sobre películas y quieres analizarla.

### Crea este dataset:
```python
peliculas = {
    'titulo': ['El Padrino', 'Pulp Fiction', 'Forrest Gump', 'Matrix', 'Inception', 
               'Gladiador', 'Titanic', 'El Caballero Oscuro', 'Avatar', 'Interstellar'],
    'año': [1972, 1994, 1994, 1999, 2010, 2000, 1997, 2008, 2009, 2014],
    'duracion_min': [175, 154, 142, 136, 148, 155, 194, 152, 162, 169],
    'puntuacion': [9.2, 8.9, 8.8, 8.7, 8.8, 8.5, 7.9, 9.0, 7.9, 8.6],
    'genero': ['Drama', 'Crimen', 'Drama', 'Ciencia Ficción', 'Ciencia Ficción',
               'Acción', 'Romance', 'Acción', 'Ciencia Ficción', 'Ciencia Ficción']
}
```

### Tareas:
1. Crea el DataFrame y explóralo
2. ¿Cuál es la película más larga? ¿Y la más corta?
3. ¿Cuál es la puntuación media de todas las películas?
4. Filtra películas con puntuación >= 8.5
5. ¿Cuántas películas hay de cada género?
6. ¿Cuál es la duración media por género?
7. Crea una columna 'epoca': 'Clásico' (antes 2000) o 'Moderno' (2000 o después)
8. Encuentra las 3 películas mejor puntuadas
9. Crea un gráfico de barras con la puntuación de cada película
10. Crea un gráfico de tarta mostrando el % de películas por género

---

## 📝 Ejercicio 2: Análisis de temperaturas

### Contexto:
Tienes datos de temperatura de una ciudad durante un mes.

### Crea o importa datos:
```python
import numpy as np
import pandas as pd

# Generar datos aleatorios de temperaturas
np.random.seed(42)
temperaturas = {
    'dia': range(1, 31),
    'temp_max': np.random.randint(18, 30, 30),
    'temp_min': np.random.randint(8, 18, 30),
    'lluvia': np.random.choice([True, False], 30, p=[0.3, 0.7])
}
```

### Tareas:
1. Calcula la temperatura media diaria (promedio de máx y mín)
2. ¿Cuál fue el día más caluroso?
3. ¿Cuántos días llovió?
4. ¿Cuál es la diferencia térmica media (temp_max - temp_min)?
5. Filtra los días con temperatura máxima > 25°C
6. Calcula la temperatura máxima media de días con y sin lluvia
7. Crea un gráfico de líneas mostrando la evolución de temp_max y temp_min
8. Añade marcadores para los días que llovió

---

## 📝 Ejercicio 3: Análisis de tienda de ropa

### Contexto:
Una tienda de ropa quiere analizar sus ventas por talla y color.

### Datos:
```python
ventas_ropa = {
    'producto': ['Camiseta', 'Pantalón', 'Sudadera', 'Chaqueta', 'Falda', 'Vestido',
                 'Camiseta', 'Pantalón', 'Camiseta', 'Sudadera'],
    'talla': ['M', 'L', 'M', 'XL', 'S', 'M', 'L', 'M', 'S', 'L'],
    'color': ['Azul', 'Negro', 'Gris', 'Negro', 'Rojo', 'Azul',
              'Blanco', 'Azul', 'Negro', 'Gris'],
    'precio': [15, 35, 28, 55, 25, 40, 15, 35, 15, 28],
    'cantidad': [10, 5, 8, 3, 12, 6, 15, 7, 9, 10]
}
```

### Tareas:
1. ¿Cuál es la talla más vendida?
2. ¿Qué color genera más ingresos?
3. ¿Cuál es el producto más caro?
4. Calcula el total vendido por producto (precio × cantidad)
5. ¿Qué producto ha generado más ingresos?
6. Agrupa por producto y calcula las unidades totales vendidas
7. Crea un gráfico de barras con las ventas por talla
8. Crea un gráfico de barras agrupadas: producto vs color

---

## 📝 Ejercicio 4: Limpieza de datos de clientes

### Contexto:
Has recibido una base de datos de clientes con errores que debes limpiar.

### Datos sucios:
```python
clientes = {
    'id': [1, 2, 3, 4, 5, 6, 7, 1],  # ID duplicado
    'nombre': ['Ana García', ' Pedro López', 'MARÍA RUIZ', 'Luis Pérez', 
               'Carmen Díaz', 'Juan Torres', None, 'Ana García'],  # Nulo y duplicado
    'email': ['ana@mail.com', 'pedro@MAIL.com', 'maria@mail.com', 'luisperez@',
              'carmen@mail.com', None, 'juan@mail.com', 'ana@mail.com'],  # Email inválido y nulo
    'edad': [25, 30, 22, 200, 28, 35, 26, 25],  # Outlier evidente
    'ciudad': ['Madrid', 'barcelona', 'VALENCIA', 'sevilla', 'Málaga', 'Bilbao', 'Madrid', 'Madrid']
}
```

### Tareas:
1. Identifica todos los problemas del dataset
2. Elimina espacios en nombres
3. Normaliza emails a minúsculas
4. Normaliza ciudades (formato título: Primera letra mayúscula)
5. Corrige o elimina la edad de 200 años (reemplaza por la media o elimina)
6. Elimina filas con nombre o email nulo
7. Elimina duplicados basándote en el ID
8. Valida que los emails contengan '@'
9. Crea un reporte de limpieza:
   - Filas originales vs finales
   - Problemas encontrados
   - Acciones tomadas

---

## 📝 Ejercicio 5: Dashboard de e-commerce

### Contexto:
Crea un dashboard visual completo de un e-commerce.

### Genera o importa datos:
```python
import pandas as pd
import numpy as np

np.random.seed(42)
n = 100  # 100 ventas

ventas_ecommerce = {
    'fecha': pd.date_range('2024-01-01', periods=n, freq='D')[:n],
    'producto': np.random.choice(['Laptop', 'Mouse', 'Teclado', 'Monitor', 'Webcam'], n),
    'categoria': np.random.choice(['Informática', 'Accesorios'], n, p=[0.6, 0.4]),
    'precio': np.random.randint(15, 800, n),
    'cantidad': np.random.randint(1, 5, n),
    'ciudad': np.random.choice(['Madrid', 'Barcelona', 'Valencia', 'Sevilla'], n)
}
df = pd.DataFrame(ventas_ecommerce)
```

### Tareas:
1. Calcula total por venta (precio × cantidad)
2. Crea 4 gráficos en un dashboard (2x2):
   - **Gráfico 1**: Evolución de ventas por mes (líneas)
   - **Gráfico 2**: Total vendido por ciudad (barras)
   - **Gráfico 3**: Distribución de precios (histograma)
   - **Gráfico 4**: Top 5 productos más vendidos (barras horizontal)
3. Añade títulos claros a cada gráfico
4. Guarda el dashboard como imagen PNG en alta resolución

---

## 📝 Ejercicio 6: Comparación de datasets

### Contexto:
Tienes ventas de dos años y quieres comparar el rendimiento.

### Datos:
```python
ventas_2023 = {
    'mes': ['Ene', 'Feb', 'Mar', 'Abr', 'May', 'Jun'],
    'ventas': [5000, 5200, 4800, 6000, 7500, 8200]
}

ventas_2024 = {
    'mes': ['Ene', 'Feb', 'Mar', 'Abr', 'May', 'Jun'],
    'ventas': [5500, 5800, 5200, 6500, 8000, 9000]
}
```

### Tareas:
1. Crea dos DataFrames
2. Calcula el crecimiento porcentual mes a mes
3. ¿En qué mes hubo mayor crecimiento?
4. Calcula las ventas totales de cada año
5. Crea un gráfico de líneas comparando ambos años
6. Añade leyenda y anotaciones

---


## 📝 Ejercicio 8: Proyecto libre

### Contexto:
Elige uno de estos temas y realiza un análisis completo:

#### Opción A: Análisis de tu música
- Crea un dataset con tus 20 canciones favoritas
- Incluye: artista, canción, género, duración, año, puntuación personal
- Analiza tus gustos musicales
- Crea visualizaciones

#### Opción B: Análisis de videojuegos
- Dataset con tus juegos favoritos
- Incluye: nombre, plataforma, género, año, horas jugadas, puntuación
- Analiza tus preferencias
- Visualiza resultados

#### Opción C: Análisis de series
- Dataset con series que has visto
- Incluye: título, género, temporadas, capítulos vistos, puntuación
- Analiza tus hábitos de visualización
- Crea gráficos

### Requisitos mínimos:
- Al menos 15 registros
- Mínimo 5 columnas
- Realizar limpieza de datos
- Calcular al menos 5 estadísticas diferentes
- Crear al menos 3 gráficos diferentes
- Documentar conclusiones

